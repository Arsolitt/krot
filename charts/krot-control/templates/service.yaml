apiVersion: v1
kind: Service
metadata:
  name: krot-cp
  labels:
    app.kubernetes.io/name: krot-cp
    app.kubernetes.io/instance: krot
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: krot-cp
